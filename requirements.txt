# --- UI ---
streamlit==1.58.0

# --- LLM & Agent ---
groq==0.37.1                 # Groq API client (used by langchain-groq)
langchain-core==1.4.6        # Base messages and tool definitions
langchain-groq==1.1.3        # LangChain wrapper for Groq LLM
langgraph==1.2.4             # Agent graph (StateGraph, MessagesState)
langgraph-checkpoint==4.1.1  # In-memory checkpointing (MemorySaver)
langgraph-prebuilt==1.1.0    # Prebuilt nodes (ToolNode)

# --- Vector DB ---
qdrant-client==1.18.0        # Vector DB client for medical KB search
sentence-transformers==5.5.1 # Embedding model for report and KB vectors

# --- Storage ---
supabase==2.31.0             # Postgres client for report/marker storage

# --- Ingestion ---
pdfplumber==0.11.9           # PDF text extraction
beautifulsoup4==4.15.0       # HTML parsing for KB source scraping
requests==2.34.2             # HTTP calls in KB source fetching

# --- Utilities ---
pydantic==2.13.4             # Data validation (BaseModel, ReportMetadata, Marker)
python-dotenv==1.2.2         # Load .env into os.environ
